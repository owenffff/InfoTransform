# ============================================================================
# REQUIRED CONFIGURATION
# ============================================================================

OPENAI_API_KEY=your-openai-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1

# ============================================================================
# OPTIONAL CONFIGURATION
# ============================================================================

# Azure Document Intelligence (Optional - For Image-Based/Scanned PDFs)
# -----------------------------------------------------------------------------
# Required for processing scanned documents and image-based PDFs that don't
# have selectable text. Standard text-based PDFs work without this.
#
# Setup Steps:
# 1. Go to Azure Portal: https://portal.azure.com/
# 2. Create "Document Intelligence" resource (formerly "Form Recognizer")
# 3. Choose pricing tier:
#    - Free: 500 pages/month
#    - Standard: ~$1.50 per 1,000 pages
# 4. Copy the endpoint URL (format: https://[resource-name].cognitiveservices.azure.com/)
# 5. Uncomment and set the variable below
#
# Authentication (choose one method):
# - Option A: Azure CLI (recommended for local dev) - Run: az login
# - Option B: Environment variables - Set AZURE_CLIENT_ID, AZURE_CLIENT_SECRET, AZURE_TENANT_ID
# - Option C: Managed Identity (automatic for Azure-hosted apps)
#
# See README.md for detailed setup instructions
#
# AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT=https://your-resource-name.cognitiveservices.azure.com/

# ============================================================================
# PORT CONFIGURATION
# ============================================================================
# IMPORTANT: Configure all three port variables to avoid port conflicts
#
# PORT                        - Frontend Next.js dev server port (Next.js auto-reads this)
# BACKEND_PORT                - Backend FastAPI server port
# NEXT_PUBLIC_BACKEND_PORT    - Must MATCH BACKEND_PORT exactly (frontend uses this to call backend API)
#
# WARNING: If these ports conflict with other services, startup will fail.
#          Ensure NEXT_PUBLIC_BACKEND_PORT matches BACKEND_PORT.

PORT=3000
BACKEND_PORT=8000
NEXT_PUBLIC_BACKEND_PORT=8000

# ============================================================================
# UPLOAD LIMIT CONFIGURATION (Optional)
# ============================================================================
# Control the maximum total size of files that can be uploaded per session.
# This prevents resource exhaustion on ephemeral deployments.
#
# Default: 1024 MB (1 GB)
# Configuration location: config/config.yaml → processing.upload.max_total_upload_size_mb
#
# To override the default, set MAX_TOTAL_UPLOAD_SIZE_MB environment variable:
# MAX_TOTAL_UPLOAD_SIZE_MB=2048  # 2 GB limit
#
# Note: This is a per-session limit (all files combined in one upload).
# Individual file size limits are controlled separately by max_file_size_mb.

# ============================================================================
# RESULT CACHING CONFIGURATION (Optional)
# ============================================================================
# Result caching avoids re-processing identical files, significantly improving performance.
# Cache entries are stored in SQLite and automatically expire based on TTL.
#
# Default values (can be overridden):
# - CACHE_ENABLED=true                # Enable/disable caching
# - CACHE_TTL_HOURS=24               # Cache lifetime in hours (0 = session-only)
# - CACHE_MAX_ENTRIES=10000          # Maximum cache entries
# - CACHE_CLEANUP_INTERVAL=6         # Hours between cleanup runs
# - CACHE_HASH_ALGO=sha256           # Hashing algorithm (sha256, sha1, md5)
# - CACHE_INVALIDATION=ttl_only      # Invalidation strategy
# - CACHE_METRICS=true               # Track cache performance metrics
# - CACHE_LOG_OPS=false              # Log cache hits/misses (useful for debugging)
# - CACHE_COMPRESS=false             # Compress cached results (saves disk space)
# - CACHE_MAX_ENTRY_SIZE=1048576     # Max entry size in bytes (1MB default)
#
# See config/caching.yaml for complete documentation and advanced settings.
#
# Example configurations:
#
# Development (No Caching):
# CACHE_ENABLED=false
#
# Production (6-Hour Cache):
# CACHE_ENABLED=true
# CACHE_TTL_HOURS=6
# CACHE_MAX_ENTRIES=5000
#
# High-Security (Session Only):
# CACHE_ENABLED=true
# CACHE_TTL_HOURS=0
# CACHE_INVALIDATION=aggressive
