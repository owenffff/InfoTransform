# OpenAI-compatible API configuration
API_KEY=your-api-key-here
BASE_URL=BASE_URL
MODEL_NAME=MODEL_NAME

# For audio transcription
WHISPER_MODEL=audio_transcription_model

# Vision prompt for image analysis (optional - uses smart default if not set)
# VISION_PROMPT=Your custom prompt here

# Server configuration
FLASK_ENV=development
FLASK_PORT=5000
